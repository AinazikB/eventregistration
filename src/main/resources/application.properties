# Spring Application Name
spring.application.name=eventregistration

# Datasource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=1234

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Email Configuration

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ainazik.bakytbek@alatoo.edu.kg
spring.mail.password=waam lpde uyce amre
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000  # Timeout for connection
spring.mail.properties.mail.smtp.timeout=5000            # Timeout for read
spring.mail.properties.mail.smtp.writetimeout=5000       # Timeout for writing

# Trust any SSL certificate (optional, for testing purposes)
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
